# Supabase (required for live articles and admin posting)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: canonical URL for share links and SEO
NEXT_PUBLIC_SITE_URL=https://spectr.com

# Editor's Desk: login is stored in DB (editor_credentials table). Optional below for one-time migration only:
# Set these once, log in at /admin/login with them; the app will create the DB row. Then you can remove these and use /admin/settings to change password.
# EDITOR_USERNAME=editor
# EDITOR_PASSWORD=your-secure-password

# Sign session cookie (required in production if you don't set EDITOR_PASSWORD). Use a long random string.
# SESSION_SECRET=random-secret-string

# Google AdSense (required for ad slots to show real ads)
NEXT_PUBLIC_ADSENSE_CLIENT_ID=ca-pub-8659207565346557
